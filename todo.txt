1. Improve update methods
    * Add comments
    * Replace if(pytype) with match
    * Make liquid have a 50/50 chance of going left/right if both are possible
2. Different heat conductivity per particle type
3. More particle types
    * Move WetSand into extra_data for sand?
    * Work on Plant more
    * Quantum "portal" particles
4. Prettier rendering (Shaders, don't use the noise effect as much / better noise effect, heat haze blur)
5. Physics


1. Figure out why line drawing sometimes leaves gaps
2. Fix hidpi handling, deal with the black border
3. Deal with window resizes


1 GUI
2. Wasm
